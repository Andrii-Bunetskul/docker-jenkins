#!groovy

pipeline {
  agent {
    label 'Built-In Node'
    }
  stages {
    stage("create docker image") {
      steps {
        echo " =================== start building image =================== "
        dir ('docker/') {
          sh 'docker build . '
        }
      }
    }
  }
}
