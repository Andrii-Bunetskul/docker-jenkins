#!groovy

pipeline {
  agent {
    label 'master'
    }
  stages {
    stage("create docker image") {
      steps {
        echo " =================== start building image =================== "
        dir ('docker/') {
          sh 'docker build --force-rm -t . '
        }
      }
    }
  }
}
